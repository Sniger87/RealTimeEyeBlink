#pragma once

static const int		SKIP_FRAMES = 1;
const float				EAR_BLINK_THRESHOLD = 0.20f;
static const int		MAX_EAR_FRAMES = 3;
static const int		RESIZED_WIDTH = 320;

static const int		MAT_WIDTH_FOR_PUPIL = 50;
static const int		WEIGHT_BLUR_SIZE = 5;
const float				WEIGHT_DIVISOR = 1.0f;
const double			GRADIENT_THRESHOLD = 50.0;
